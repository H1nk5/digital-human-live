# 核心依赖
openai>=1.0.0
pyyaml>=6.0
edge-tts>=6.1.0
aiohttp>=3.9.0
websockets>=12.0
protobuf>=4.25.0
numpy>=1.24.0
Pillow>=10.0.0

# TTS（可选，按需安装）
# cosyvoice    # pip install cosyvoice 或从源码安装
# gpt-sovits   # 从 GPT-SoVITS 仓库安装

# 数字人（可选，按需安装）
# musetalk     # 从 MuseTalk 仓库安装
# sadtalker    # 从 SadTalker 仓库安装

# 推流
# ffmpeg       # 系统安装，不在 pip 里
